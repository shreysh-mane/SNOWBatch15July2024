### Q1
Access and print the third element of the array.
```javascript
function printThirdElement() {
    var array1 = [10, 20, 30, 40, 50];
    // Add your code here to print the third element i.e 30.
    
}
printThirdElement();
```

### Q2
Change the second element of the array to 50 and print the updated array.
```javascript
function changeSecondElement() {
    var array2 = [5, 15, 25, 35, 45];
    // Add your code here to change the second element to 50
   
    console.log(array2);
}
changeSecondElement();
```

### Q3
Add the number 60 to the end of the array and print the updated array.
```javascript
function addElementToEnd() {
    var array3 = [1, 2, 3, 4, 5];
    // Add your code here to add 60 to the end of the array [hint: push()]
   
    console.log(array3);
}
addElementToEnd();
```

### Q4
Add the number 0 to the beginning of the array and print the updated array.
```javascript
function addElementToBeginning() {
    var array4 = [1, 2, 3, 4, 5];
    // Add your code here to add 0 to the beginning of the array
    
    console.log(array4);
}
addElementToBeginning();
```

### Q5
Remove the last element of the array and print the updated array.
```javascript
function removeLastElement() {
    var array5 = [10, 20, 30, 40, 50];
    // Add your code here to remove the last element
    
    console.log(array5);
}
removeLastElement();
```

### Q6
Remove the first element of the array and print the updated array.
```javascript
function removeFirstElement() {
    var array6 = [10, 20, 30, 40, 50];
    // Add your code here to remove the first element
    
    console.log(array6);
}
removeFirstElement();
```

### Q7
Remove 2 elements starting from index 1 and print the updated array.
```javascript
function removeTwoElements() {
    var array7 = [100, 200, 300, 400, 500];
    // Add your code here to remove 2 elements starting from index 1 [Hint: splice()]
   
    console.log(array7);
}
removeTwoElements();
```

### Q8
Add elements 600 and 700 at index 2 and print the updated array.
```javascript
function addElementsAtIndex() {
    var array8 = [100, 200, 300, 400, 500];
    // Add your code here to add 600 and 700 at index 2
    
    console.log(array8);
}
addElementsAtIndex();
```

### Q9
Find the index of the element 400 in the array and print the index.
```javascript
function findElementIndex() {
    var array9 = [100, 200, 300, 400, 500];
    // Add your code here to find the index of 400
    
    console.log(index);
}
findElementIndex();
```

### Q10
Use a for loop to iterate over the array and print each element.
```javascript
function printEachElement() {
    var array10 = ['a', 'b', 'c', 'd', 'e'];
    // Complete the for loop to iterate over the array and print each element
    for () {
        console.log(array10[i]);
    }
}
printEachElement();
```

### Q11: Find the maximum number in an array
```javascript
function findMaxNumber(arr) {
    // Add your code here to find the maximum number in the array
    
    console.log(maxNumber);
}
findMaxNumber([1, 2, 3, 4, 5]); 
```

### ### Arrow Function

### Q12: Calculate the sum of an array of numbers
```javascript
const sumArray = (arr) => {
    // Add your code here to calculate the sum of the array
    
    console.log(sum);
}
sumArray([1, 2, 3, 4, 5]); 
```